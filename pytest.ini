[pytest]
testpaths = tests
addopts = -m "not integration"
markers =
    integration: requires generated outputs from a completed pipeline run

# Spark-based verification runners live in spark_jobs/ and run inside Docker.
# They are intentionally excluded from the host-side pytest suite.
